---
name: graph
description: >
  Generate or update CODEBASE_GRAPH.md — a structured map of the existing codebase for fast
  navigation, onboarding, and token-efficient file search. Activate when user says "graph",
  "map codebase", "graph my project", "visualise project structure", "codebase overview", or runs /graph.
  Output: CODEBASE_GRAPH.md that Claude reads instead of re-scanning files each session.
  Also auto-updates mid-session whenever new files, routes, models, or dependencies are added.
---

# Graph — Codebase Map Generator

Generate `CODEBASE_GRAPH.md`: a machine- and human-readable map Claude references instead of
re-reading files each session. Cuts token usage by replacing repeated directory scans with graph lookups.

---

## Phase 1 — Deep Scan

Walk the working directory. Collect:

### Structure layer
- All directories up to 4 levels deep
- Skip: `node_modules`, `.git`, `dist`, `build`, `.next`, `__pycache__`, `venv`, `.turbo`, `coverage`
- File counts per directory
- Entry points: `main.*`, `index.*`, `app.*`, `server.*`, `_app.*`, `layout.*`

### Dependency layer
- Parse `package.json` / `pyproject.toml` / `go.mod` / `Cargo.toml` / `*.csproj` / `pom.xml`
- Separate: runtime deps / dev deps / peer deps
- Flag deprecated, outdated, or security-flagged packages if lockfile present

### Module relationship layer
- Static import analysis on top 2 levels — do not read every file
- What imports what
- Shared utilities / lib folders and who uses them
- API route surface: list all route files/handlers found
- DB layer: models, schemas, migrations found

### Config layer
- Env vars referenced — scan `.env.example`, config files. **Never read `.env`**
- Build/deploy configs detected
- Test config and coverage setup

---

## Phase 2 — Generate CODEBASE_GRAPH.md

Write the file with this structure (populate all sections from scan):

```markdown
## 🤖 Claude Usage Instructions
When working in this codebase:
1. Read CODEBASE_GRAPH.md FIRST — use it to locate files before reading them.
2. Do NOT read entire directories. Use the Directory Map to identify the exact file.
3. Use the Navigation Quick-Reference to find where to add new code.
4. Re-read this file (not source files) when asked "where is X" or "how does Y work at high level".
5. Regenerate with `/graph` only after major structural refactors.

# CODEBASE_GRAPH.md — {{ PROJECT_NAME }}
> Auto-generated by project-pilot /graph on {{ DATE }}
> Reference this file instead of re-reading source files for navigation and context.
> Regenerate with `/graph` after major structural changes.

## 📁 Directory Map
[annotated tree — 4 levels max, file counts per dir, entry points marked]

## 🚪 Entry Points
| File | Purpose |
|---|---|
| ... | ... |

## 🔗 Module Dependency Map
[which top-level modules import which]

## 🛣️ API / Route Surface
| Route | Method | Handler file | Auth required |
|---|---|---|---|
| ... | ... | ... | ... |

## 🗄️ Data Layer
**ORM:** ...
**Models:** ...
**Schema:** ...
**Migrations:** ...

## 📦 Key Dependencies
| Package | Version | Purpose |
|---|---|---|
| ... | ... | ... |

## 🌍 Environment Variables
| Variable | Required | Used in |
|---|---|---|
| ... | ... | ... |

## ⚙️ Build & Deploy
- **Build:** ...
- **Deploy target:** ...
- **CI:** ...
- **Tests:** ...

## 🧭 Navigation Quick-Reference
| I need to… | Go to |
|---|---|
| ... | ... |
```

---

## Mid-Session Auto-Update Rules

When any of these happen, **patch CODEBASE_GRAPH.md immediately** — do not wait for session end:

| Event | Section to patch |
|---|---|
| New file or directory created | Directory Map |
| File deleted or renamed | Directory Map |
| New route or API endpoint added | API / Route Surface |
| New DB model or migration | Data Layer |
| New package installed | Key Dependencies |
| New component/module/lib scaffolded | Directory Map + Navigation Quick-Reference |

**Patch rules:**
- Do NOT regenerate the full file — edit only the affected section
- Append to top of file: `> Last updated: {{ DATE }} — {{ what changed }}`

---

## Phase 3 — Handoff

1. Confirm `CODEBASE_GRAPH.md` written
2. Report: directories scanned, files counted, sections generated
3. Tell the user: *"Reference CODEBASE_GRAPH.md at session start for fast navigation. Claude will check this file before reading any source file."*
4. If no CLAUDE.md or PROJECT_MEMORY.md found, suggest: *"Run `/pilot-init` to also generate CLAUDE.md and PROJECT_MEMORY.md."*
